import { ConfirmationModalContentDirective } from './confirmation-modal-content.directive';

describe('ConfirmationModalContentDirective', () => {
  it('should create an instance', () => {
    const directive = new ConfirmationModalContentDirective();
    expect(directive).toBeTruthy();
  });
});
