import { ConfirmationModalTitleDirective } from './confirmation-modal-title.directive';

describe('ConfirmationModalTitleDirective', () => {
  it('should create an instance', () => {
    const directive = new ConfirmationModalTitleDirective();
    expect(directive).toBeTruthy();
  });
});
