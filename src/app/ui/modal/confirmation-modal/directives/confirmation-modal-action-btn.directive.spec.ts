import { ConfirmationModalActionBtnDirective } from './confirmation-modal-action-btn.directive';

describe('ConfirmationModalActionBtnTextDirective', () => {
  it('should create an instance', () => {
    const directive = new ConfirmationModalActionBtnDirective();
    expect(directive).toBeTruthy();
  });
});
